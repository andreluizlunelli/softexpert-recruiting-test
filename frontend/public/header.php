<!DOCTYPE html>
<html>
<head>
    <title>SoftExpert market</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8">
    <style>
        :root {
            --cor-principal: #039be5;
            --cor-principal-active: #0277bd;
            --cor-cinza-escuro: #bdbdbd;
        }
        .container {
            color: var(--cor-principal);
        }
        input {
            border-bottom: 1px solid var(--cor-cinza-escuro) !important;
        }
        input:active, input:hover, input:focus {
            border-bottom: 1px solid var(--cor-principal-active) !important;
        }
        .material-icons.active {
            color: var(--cor-principal-active) !important;
        }

    </style>
</head>